#2. Encontrar a soma dos valores em uma tupla:
#Modifique o código para calcular a soma de todos os valores na tupla.

tupla = (5, 6, 7, 8, 9)
soma = sum(tupla)
print("a soma de todos os numeros é:", soma)